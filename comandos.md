    1  sudo apt install terminator
    9  sudo apt install firefox
   49  sudo apt install gcc
   56  sudo apt install gcc
   63  sudo apt install openjdk-21-jre openjdk-21-jdk
   75  sudo apt install python3
  127  sudo snap install code
  128  sudo snap install code --classic
  237  sudo apt install kotlin
  276  sudo apt install maven openjdk-17-jdk openjdk-17-jre
  292  sudo apt install simplescreenrecorder
  346  history | grep install
  347  history | grep install > comandos.md
